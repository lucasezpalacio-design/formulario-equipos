<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Formulario de ingreso de equipos</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f5f7fa;
      color: #333;
      margin: 0;
      padding: 20px;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      background-color: #fff;
      padding: 25px;
      border-radius: 12px;
      box-shadow: 0 0 12px rgba(0, 0, 0, 0.1);
    }
    h2 {
      text-align: center;
      color: #1e40af;
    }
    label {
      display: block;
      margin-top: 15px;
      font-weight: bold;
    }
    input, textarea, select {
      width: 100%;
      padding: 10px;
      margin-top: 5px;
      border: 1px solid #ccc;
      border-radius: 8px;
      box-sizing: border-box;
      font-size: 15px;
    }
    textarea {
      resize: vertical;
    }
    button {
      display: block;
      width: 100%;
      margin-top: 20px;
      padding: 12px;
      background-color: #1e40af;
      color: white;
      border: none;
      border-radius: 8px;
      font-size: 16px;
      cursor: pointer;
    }
    button:hover {
      background-color: #1d4ed8;
    }
    .consent {
      display: flex;
      align-items: center;
      margin-top: 15px;
    }
    .consent input {
      width: auto;
      margin-right: 10px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>Formulario de ingreso de equipos</h2>
    <form id="formIngreso">
      <label>Nombre del cliente</label>
      <input type="text" name="nombre" required>

      <label>Correo electrónico</label>
      <input type="email" name="correo" required>

      <label>Número de teléfono</label>
      <input type="tel" name="telefono" required>

      <label>Tipo de equipo</label>
      <select name="tipo_equipo" required>
        <option value="">Seleccione...</option>
        <option>PC de escritorio</option>
        <option>Notebook</option>
      </select>

      <label>Marca</label>
      <input type="text" name="marca" required>

      <label>Modelo</label>
      <input type="text" name="modelo" required>

      <label>Accesorios entregados</label>
      <input type="text" name="accesorios" placeholder="Ej: cargador, mouse, cables...">

      <label>Tipo de servicio</label>
      <select name="servicio" required>
        <option value="">Seleccione...</option>
        <option>Mantenimiento</option>
        <option>Arreglo / Reparación</option>
        <option>Limpieza</option>
      </select>

      <label>Descripción del problema o solicitud</label>
      <textarea name="descripcion" rows="4" required></textarea>

      <div class="consent">
        <input type="checkbox" name="consentimiento" required>
        <label for="consentimiento">Acepto las condiciones del servicio y autorizo la revisión del equipo.</label>
      </div>

      <button type="submit">Enviar formulario</button>
    </form>
  </div>

  <script>
    document.getElementById('formIngreso').addEventListener('submit', function(event) {
      event.preventDefault();
      alert('Formulario enviado correctamente. ¡Gracias por su confianza!');
      this.reset();
    });
  </script>
</body>
</html>
